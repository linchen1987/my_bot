# My Bot

## Commands

```bash
make install       # Install main dependencies
make dev-install   # Install main and dev dependencies (includes pytest)
make test          # Run tests with pytest
make test-cov      # Run tests with coverage
make lint          # Run linter
make run           # Run the main script
make sync-deps     # Sync dependencies with uv
make clean         # Clean venv and cache
```